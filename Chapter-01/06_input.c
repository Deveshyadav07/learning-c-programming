/* Syntax: scanf("%d", &i);
*/
#include <stdio.h>
int main(){
    /*
    question 01) take a input(integer type) from user and run your code?
    */
    int a; // variables declared
    scanf("%d", &a);
    printf("the value of a is : %d", a);

    /*
    question 02) take a input(charactor type) from user ? and what is output?

    Hint -
    char b;
    scanf("%c", &b);
    printf("the value of b is : %c", b);
    */
    return 0;
}
