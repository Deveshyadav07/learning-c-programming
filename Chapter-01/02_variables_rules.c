#include <stdio.h>
int main(){
    int a;
    int b;
    int glass; // they are all allowed 

    /* RULES FOR NAMING VARIABLES IN C

    1. First character must be an alphabet or underscore (_)
    2. No commas, blanks are allowed.
    3. No special symbol other than (_) allowed.
    4. Variable names are case sensitive.
    */

    /*
    question - 01) int dev-yadav; allowed or not?
    question - 02) int dev_yadav; allowed or not?
    question - 03) int dev&yadav; allowed or not?

    */
    int k = 7;
    printf("%d", k); //output 7
    return 0;
}